function isPrime(n) {
    if (n === 1) return false
    // current goes from 2 - n
    // check if current is crossed off
    // if current is not crossed off 
    // ----1---it is prime-----1----
    // ----2---cross off all multiples smaller than n----2---
}

module.exports = { isPrime }

